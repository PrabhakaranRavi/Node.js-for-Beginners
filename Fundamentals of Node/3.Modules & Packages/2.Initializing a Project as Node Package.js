/**
 * you learnt that packages are nothing but different modules packaged together and is described by a package.json file. 
 * A package can be used by other packages. Node.js uses NPM, which is the Node Package Manager, to find and install thousands of packages. 
 * NPM is one of the largest repositories consisting of different packages and modules. 
 * You will find packages in almost all use cases.
 */

//npm init